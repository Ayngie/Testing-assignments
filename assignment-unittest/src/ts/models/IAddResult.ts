export interface IAddResponse {
  //Interface som exporteras till vår funktion addTodo - för att vår response från vår addTodofunktion
  //Två egenskaper i denna interface, samt deras datatyper:
  success: boolean;
  error: string;
}
